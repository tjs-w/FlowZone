interface:
  display_name: "CallFlow"
  short_description: "Explore evidence-backed local code workflows"
  brand_color: "#0F766E"
  default_prompt: "Use $callflow:callflow to map this entry point to its sink with verified evidence."

policy:
  allow_implicit_invocation: true
