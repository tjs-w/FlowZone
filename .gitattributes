# Deterministic generated shipping artifacts. Preserve their exact bytes across
# platforms instead of allowing core.autocrlf to rewrite a Windows checkout.
server/dist/** binary
web/dist/** binary

# Preserve approved third-party license bytes, including upstream terminal blank lines.
licenses/callflow/** -text
licenses/callflow/*-LICENSE.txt whitespace=-blank-at-eof
